version: 0.0
os: linux
files:
  - source: /
    destination: /tmp
hooks:
  ApplicationStart:
    - location: start_jar.sh
      timeout: 60
      runas: root
  ValidateService:
    - location: health_check.sh
      timeout: 30